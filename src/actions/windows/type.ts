export const CLIP_HISTORY = '__clip_history';

export enum WinActEnum {
  HIDE_WIN = 'hide-window',
  SHOW_WIN = 'show-window',
  PASTE = 'paste',
  None = '',
}

export enum StoreEnum {
  SET_STORE = 'set-store',
  GET_STORE = 'get-store',
}
